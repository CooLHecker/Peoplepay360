import { ArrowRight, CheckCircle2, FileText, Play, WalletCards } from "lucide-react";
import { useState } from "react";
import { PageHeading, Status, money } from "@/components/ui/PageBits";

const runs = [{ period: "August 2026", employees: 124, gross: 1824500, status: "Completed" }, { period: "July 2026", employees: 121, gross: 1768200, status: "Completed" }, { period: "June 2026", employees: 119, gross: 1713900, status: "Completed" }];

export default function PayrollPage() {
  const [step, setStep] = useState(1);
  const [started, setStarted] = useState(false);
  return <div className="mx-auto max-w-[1440px]"><PageHeading eyebrow="Compensation" title="Payroll" description="Prepare, review, and run payroll with confidence." action={<button onClick={() => { setStarted(true); setStep(1); }} className="inline-flex items-center gap-2 rounded-lg bg-[#714b67] px-4 py-2.5 text-sm font-bold text-white"><Play size={16} />Run new payroll</button>} /><div className="grid gap-6 xl:grid-cols-[1.1fr_1fr]"><section className="rounded-xl border border-[#e6e0e5] bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04)]"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#f3edf2] text-[#714b67]"><WalletCards size={20} /></span><div><h2 className="text-lg font-bold text-[#352f37]">September payroll</h2><p className="text-sm text-[#9c8e99]">Pay date: September 30, 2026</p></div></div><div className="mt-8 flex items-center">{["Review", "Employees", "Process"].map((label, index) => <div key={label} className="flex flex-1 items-center"><div className={`grid h-8 w-8 place-items-center rounded-full text-xs font-bold ${step > index ? "bg-[#714b67] text-white" : "border-2 border-[#d8c4d3] text-[#714b67]"}`}>{step > index ? <CheckCircle2 size={16} /> : index + 1}</div><span className="ml-2 text-xs font-bold text-[#756c75]">{label}</span>{index < 2 && <div className={`mx-3 h-px flex-1 ${step > index + 1 ? "bg-[#714b67]" : "bg-[#e6e0e5]"}`} />}</div>)}</div><div className="mt-8 rounded-xl bg-[#fbf8fa] p-5"><p className="text-xs font-bold uppercase tracking-wider text-[#9c8e99]">{step === 1 ? "Step 1: review payroll" : step === 2 ? "Step 2: select employees" : "Step 3: process payroll"}</p><p className="mt-3 text-2xl font-bold text-[#352f37]">{step === 3 ? money(1842000) : step === 2 ? "124 employees" : "Ready to review"}</p><p className="mt-1 text-sm text-[#756c75]">{step === 1 ? "All active employees are included in this pay run." : step === 2 ? "No employees are excluded from this pay run." : "Review totals before creating the pay run."}</p></div><button onClick={() => { if (step < 3) setStep(step + 1); else setStarted(false); }} className="mt-6 inline-flex items-center gap-2 rounded-lg bg-[#714b67] px-5 py-3 text-sm font-bold text-white">{step < 3 ? "Continue" : "Create pay run"}<ArrowRight size={16} /></button>{started && <span className="ml-3 text-sm font-bold text-[#27804d]">Changes saved locally</span>}</section><section className="rounded-xl border border-[#e6e0e5] bg-white p-6 shadow-[0_1px_3px_rgba(0,0,0,0.04)]"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold text-[#352f37]">Payroll history</h2><p className="mt-1 text-sm text-[#9c8e99]">Your latest pay runs</p></div><FileText size={20} className="text-[#9c8e99]" /></div><div className="mt-5 divide-y divide-[#f0ebef]">{runs.map((run) => <div key={run.period} className="flex items-center justify-between gap-3 py-4"><div><p className="text-sm font-bold text-[#352f37]">{run.period}</p><p className="mt-1 text-xs text-[#9c8e99]">{run.employees} employees · {money(run.gross)}</p></div><Status>{run.status}</Status></div>)}</div></section></div></div>;
}
