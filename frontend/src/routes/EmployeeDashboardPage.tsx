import { Bot, CalendarCheck2, Camera, CheckCircle2, Clock3, FileText, LogOut, MapPin, MessageCircle, Send, WalletCards, X } from "lucide-react";
import { ChangeEvent, FormEvent, useState } from "react";
import type { ReactNode } from "react";
import { Link, useNavigate } from "react-router-dom";
import { Metric, PageHeading, Status, money } from "@/components/ui/PageBits";
import { useAuth } from "@/lib/auth";

const monthAttendance = [{ date: "Sep 05", day: "Friday", checkIn: "08:54 AM", checkOut: "--", hours: "In progress", status: "Present" }, { date: "Sep 04", day: "Thursday", checkIn: "09:02 AM", checkOut: "05:41 PM", hours: "8h 39m", status: "Present" }, { date: "Sep 03", day: "Wednesday", checkIn: "08:48 AM", checkOut: "05:36 PM", hours: "8h 48m", status: "Present" }, { date: "Sep 02", day: "Tuesday", checkIn: "--", checkOut: "--", hours: "--", status: "On leave" }, { date: "Sep 01", day: "Monday", checkIn: "09:18 AM", checkOut: "05:22 PM", hours: "8h 04m", status: "Late" }];
const timeOff = [{ type: "Annual leave", dates: "Sep 12 - Sep 16, 2026", days: "5 days", status: "Pending" }, { type: "Personal day", dates: "Aug 22, 2026", days: "1 day", status: "Approved" }];
const answers: Record<string, string> = { leave: "You have 14 annual leave days available. Requests are usually reviewed by your manager within two working days.", maternity: "Maternity leave is available according to your company policy and applicable local regulations. Contact People Operations for eligibility and documentation.", sick: "For sick leave, submit a request with the dates and a note. Your manager or People Operations can help with supporting documents.", payroll: "Payslips are published after payroll is validated. Your latest payslip is August 2026 and the net salary is ₹1,28,400." };

export default function EmployeeDashboardPage() {
  const { session, signOut } = useAuth();
  const navigate = useNavigate();
  const name = session?.user.full_name ?? "Team member";
  const [attendanceOpen, setAttendanceOpen] = useState(false);
  const [checkedIn, setCheckedIn] = useState(true);
  const [photo, setPhoto] = useState("");
  const [coordinates, setCoordinates] = useState("Location not captured");
  const [chatOpen, setChatOpen] = useState(false);
  const [question, setQuestion] = useState("");
  const [messages, setMessages] = useState([{ from: "bot", text: "Hi! Ask me about leave, maternity leave, sick leave, or payroll." }]);
  const capturePhoto = (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (file) setPhoto(file.name); if (navigator.geolocation) navigator.geolocation.getCurrentPosition((position) => setCoordinates(`${position.coords.latitude.toFixed(5)}, ${position.coords.longitude.toFixed(5)}`), () => setCoordinates("Location permission was not granted")); };
  const ask = (event: FormEvent) => { event.preventDefault(); if (!question.trim()) return; const query = question.toLowerCase(); const key = ["maternity", "payroll", "sick", "leave"].find((item) => query.includes(item)); setMessages((current) => [...current, { from: "user", text: question }, { from: "bot", text: key ? answers[key] : "I can help with leave balance, maternity leave, sick leave, and payroll questions. For anything else, contact People Operations." }]); setQuestion(""); };
  return <div className="min-h-screen bg-[#f7f5f6] px-5 py-7 sm:px-8 lg:px-12"><div className="mx-auto max-w-[1250px]"><header className="mb-10 flex items-center justify-between"><Link to="/my-dashboard" className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#714b67] text-lg font-bold text-white">P</span><strong className="text-lg text-[#5c3a54]">PeoplePay</strong><span className="hidden rounded-full bg-[#f3edf2] px-2.5 py-1 text-xs font-bold text-[#714b67] sm:inline">Employee workspace</span></Link><button onClick={() => { void signOut().then(() => navigate("/employee-login")); }}
   className="inline-flex items-center gap-2 text-sm font-bold text-[#756c75]">
    <LogOut size={16} /> Log out</button>
    </header>
    <PageHeading eyebrow="My workspace" title={`Good morning, ${name.split(" ")[0]}`} description="Your attendance, leave, payroll, contract, and time-off information." />
    <div className="grid gap-4 sm:grid-cols-3">
      <Metric label="Today's attendance" value={checkedIn ? "Present" : "Not checked in"} detail={checkedIn ? "08:54 AM" : "Start your day"} tone="green" /><Metric label="Leave balance" value="14 days" detail="Available" tone="plum" /><Metric label="Latest payslip" value={money(128400)} detail="Paid" tone="blue" /></div><section className="mt-6 rounded-xl border border-[#e6e0e5] bg-white p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><div className="flex items-center gap-2"><CalendarCheck2 size={20} className="text-[#714b67]" /><h2 className="text-lg font-bold text-[#352f37]">Today's attendance</h2></div><p className="mt-1 text-sm text-[#9c8e99]">Add a geotagged photo when you check in or out.</p></div><div className="flex gap-2"><button onClick={() => setAttendanceOpen(true)} className="inline-flex items-center gap-2 rounded-lg border border-[#e6e0e5] px-4 py-2.5 text-sm font-bold text-[#714b67]"><Camera size={16} /> {checkedIn ? "Check out" : "Check in"}</button><button onClick={() => setAttendanceOpen(true)} className="inline-flex items-center gap-2 rounded-lg bg-[#714b67] px-4 py-2.5 text-sm font-bold text-white"><MapPin size={16} /> Add attendance</button></div></div><div className="mt-6 grid gap-4 sm:grid-cols-4"><Info icon={<Clock3 size={16} />} label="Check in" value="08:54 AM" /><Info icon={<Clock3 size={16} />} label="Check out" value={checkedIn ? "Not yet" : "05:45 PM"} /><Info icon={<Clock3 size={16} />} label="Worked hours" value={checkedIn ? "In progress" : "8h 51m"} /><Info icon={<CheckCircle2 size={16} />} label="Status" value="On schedule" /></div></section><div className="mt-6 grid gap-6 lg:grid-cols-[1.35fr_1fr]"><section className="rounded-xl border border-[#e6e0e5] bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="text-lg font-bold text-[#352f37]">Attendance this month</h2><p className="mt-1 text-sm text-[#9c8e99]">September 2026</p></div><Status>92% healthy</Status></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[580px] text-left"><thead className="border-b border-[#eee9ed] text-xs uppercase tracking-wider text-[#9c8e99]"><tr><th className="px-3 py-3">Date</th><th className="px-3 py-3">Check in</th><th className="px-3 py-3">Check out</th><th className="px-3 py-3">Hours</th><th className="px-3 py-3">Status</th></tr></thead><tbody className="divide-y divide-[#f0ebef]">{monthAttendance.map((row) => <tr key={row.date}><td className="px-3 py-3"><strong className="block text-sm text-[#352f37]">{row.date}</strong><small className="text-xs text-[#9c8e99]">{row.day}</small></td><td className="px-3 py-3 text-sm text-[#756c75]">{row.checkIn}</td><td className="px-3 py-3 text-sm text-[#756c75]">{row.checkOut}</td><td className="px-3 py-3 text-sm text-[#756c75]">{row.hours}</td><td className="px-3 py-3"><Status>{row.status}</Status></td></tr>)}</tbody></table></div></section><section className="rounded-xl border border-[#e6e0e5] bg-white p-6"><h2 className="text-lg font-bold text-[#352f37]">My time off requests</h2><p className="mt-1 text-sm text-[#9c8e99]">Requests and their current status</p><div className="mt-5 space-y-3">{timeOff.map((request) => <div key={request.dates} className="rounded-lg bg-[#fbf8fa] p-4"><div className="flex items-center justify-between gap-2"><p className="text-sm font-bold text-[#352f37]">{request.type}</p><Status>{request.status}</Status></div><p className="mt-2 text-xs text-[#756c75]">{request.dates} · {request.days}</p></div>)}</div><Link to="/time-off" className="mt-5 inline-block text-sm font-bold text-[#714b67]">Request time off</Link></section></div><div className="mt-6 grid gap-6 md:grid-cols-2"><section className="rounded-xl border border-[#e6e0e5] bg-white p-6"><div className="flex items-center gap-3"><WalletCards size={20} className="text-[#714b67]" /><div><h2 className="text-lg font-bold text-[#352f37]">My payroll</h2><p className="text-sm text-[#9c8e99]">Latest payslip and payment details</p></div></div><div className="mt-6 grid grid-cols-3 gap-3"><Info label="Pay period" value="August 2026" /><Info label="Net salary" value="₹1,28,400" /><Info label="Status" value="Paid" /></div><button className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-[#714b67]"><FileText size={16} /> View / download payslip</button></section><section className="rounded-xl border border-[#e6e0e5] bg-white p-6"><div className="flex items-center gap-3"><FileText size={20} className="text-[#714b67]" /><div><h2 className="text-lg font-bold text-[#352f37]">My contract</h2><p className="text-sm text-[#9c8e99]">Your ongoing employment agreement</p></div><Status>Running</Status></div><div className="mt-6 grid grid-cols-2 gap-4"><Info label="Job position" value="Product Designer" /><Info label="Start date" value="Jan 15, 2024" /><Info label="Work schedule" value="Standard 40h" /><Info label="Deal status" value="Active" /></div></section></div></div>{attendanceOpen && <AttendanceModal photo={photo} coordinates={coordinates} onPhoto={capturePhoto} onClose={() => setAttendanceOpen(false)} onSave={() => { setCheckedIn((value) => !value); setAttendanceOpen(false); }} />}{chatOpen && <Chat messages={messages} question={question} setQuestion={setQuestion} onAsk={ask} onClose={() => setChatOpen(false)} />}<button onClick={() => setChatOpen(true)} className="fixed bottom-6 right-6 grid h-14 w-14 place-items-center rounded-full bg-[#714b67] text-white shadow-xl shadow-[#714b67]/30" aria-label="Open leave assistant"><MessageCircle size={23} /></button></div>;
}

function Info({ icon, label, value }: { icon?: ReactNode; label: string; value: string }) { return <div><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-[#9c8e99]">{icon}{label}</p><p className="mt-2 text-sm font-semibold text-[#352f37]">{value}</p></div>; }
function AttendanceModal({ photo, coordinates, onPhoto, onClose, onSave }: { photo: string; coordinates: string; onPhoto: (event: ChangeEvent<HTMLInputElement>) => void; onClose: () => void; onSave: () => void }) { return <div className="fixed inset-0 z-40 grid place-items-center bg-[#25212a]/30 p-4"><section className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><h2 className="text-xl font-bold text-[#352f37]">Add geotagged attendance</h2><p className="mt-1 text-sm text-[#9c8e99]">Take or choose a photo and allow location access.</p></div><button onClick={onClose} aria-label="Close"><X size={18} /></button></div><label className="mt-6 flex cursor-pointer flex-col items-center rounded-xl border-2 border-dashed border-[#d8c4d3] bg-[#fbf8fa] p-8"><Camera className="text-[#714b67]" size={28} /><strong className="mt-3 text-sm text-[#352f37]">Choose attendance photo</strong><span className="mt-1 text-xs text-[#9c8e99]">Use your camera or upload an image</span><input type="file" accept="image/*" capture="environment" className="hidden" onChange={onPhoto} /></label>{photo && <p className="mt-4 text-sm font-semibold text-[#352f37]">Photo: {photo}</p>}<p className="mt-3 flex items-center gap-2 rounded-lg bg-[#eef5fb] p-3 text-xs font-semibold text-[#3d6f99]"><MapPin size={15} /> {coordinates}</p><div className="mt-6 flex justify-end gap-2"><button onClick={onClose} className="rounded-lg px-4 py-2.5 text-sm font-bold text-[#756c75]">Cancel</button><button onClick={onSave} disabled={!photo} className="rounded-lg bg-[#714b67] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-40">Save attendance</button></div></section></div>; }
function Chat({ messages, question, setQuestion, onAsk, onClose }: { messages: { from: string; text: string }[]; question: string; setQuestion: (value: string) => void; onAsk: (event: FormEvent) => void; onClose: () => void }) { return <section className="fixed bottom-24 right-5 z-30 flex h-[470px] w-[min(380px,calc(100vw-2.5rem))] flex-col overflow-hidden rounded-2xl border border-[#e6e0e5] bg-white shadow-2xl"><header className="flex items-center justify-between bg-[#714b67] p-4 text-white"><div className="flex items-center gap-2"><Bot size={19} /><strong>PeoplePay assistant</strong></div><button onClick={onClose} aria-label="Close assistant"><X size={18} /></button></header><div className="flex-1 space-y-3 overflow-y-auto p-4">{messages.map((message, index) => <p key={`${message.from}-${index}`} className={`max-w-[85%] rounded-xl p-3 text-sm leading-5 ${message.from === "user" ? "ml-auto bg-[#f3edf2] text-[#5c3a54]" : "bg-[#fbf8fa] text-[#756c75]"}`}>{message.text}</p>)}</div><form onSubmit={onAsk} className="flex gap-2 border-t border-[#eee9ed] p-3"><input value={question} onChange={(event) => setQuestion(event.target.value)} placeholder="Ask about leave or payroll" className="min-w-0 flex-1 rounded-lg border border-[#e6e0e5] px-3 text-sm outline-none focus:border-[#714b67]" /><button className="grid h-10 w-10 shrink-0 place-items-center rounded-lg bg-[#714b67] text-white" aria-label="Send question"><Send size={16} /></button></form></section>; }
